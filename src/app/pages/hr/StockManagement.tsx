import { StockManagement } from "../StockManagement";

/** HR embedded stock overview — same layout, links to HR category routes */
export function HRStockManagement() {
  return <StockManagement hrMode />;
}
